name: fetch-investigation
description: Poll investigation status, logs, and final report
type: http
config:
  method: GET
  url: "${GHOSTTRACE_API_URL:-http://localhost:8000}/investigation/{{investigation_id}}"
